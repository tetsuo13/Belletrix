@using Belletrix.Entity.Model;

@model StudentModel

@{
    ViewBag.Title = "View";
}

@section FooterScript {
    <script>
        $(function () {
            new Belletrix.Student().initView('@Url.Action("Delete", "Student")', 'studentid',
                '@Url.Action("List", "Student")');
            new Belletrix.StudentNote();
        });
    </script>
}

@section Body {
    <br />
    @Html.Partial("View.Partial", Model)
}
